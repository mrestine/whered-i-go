{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/rides/:id", "destination": "/api/rides?id=:id" },
    { "source": "/((?!api/|assets/).*)", "destination": "/index.html" }
  ]
}
